# Data Ingestion Application Configuration
spring.application.name=financial-analytics-data-ingestion

# Server Configuration (different port to avoid conflicts)
server.port=8081

# Logging
logging.level.root=INFO
logging.level.com.financial.analytics=DEBUG

# ClickHouse Configuration (to be configured)
# clickhouse.url=jdbc:clickhouse://localhost:8123/financial_data
# clickhouse.username=default
# clickhouse.password=
