<clickhouse>
    <!-- Custom configuration for Financial Analytics ClickHouse instance -->
    
    <!-- Network settings -->
    <listen_host>0.0.0.0</listen_host>
    
    <!-- Logging -->
    <logger>
        <level>information</level>
        <console>true</console>
    </logger>
    
    <!-- Performance settings optimized for time-series analytics -->
    <max_concurrent_queries>100</max_concurrent_queries>
    
    <!-- Compression settings for better storage efficiency -->
    <compression>
        <case>
            <method>zstd</method>
            <level>3</level>
        </case>
    </compression>
    
    <!-- Time zone -->
    <timezone>UTC</timezone>
    
    <!-- Background job settings -->
    <background_pool_size>16</background_pool_size>
    <background_schedule_pool_size>16</background_schedule_pool_size>
    <background_message_broker_schedule_pool_size>16</background_message_broker_schedule_pool_size>
    
    <!-- Merge settings -->
    <merge_tree>
        <max_suspicious_broken_parts>5</max_suspicious_broken_parts>
        <parts_to_throw_insert>3000</parts_to_throw_insert>
        <parts_to_delay_insert>1000</parts_to_delay_insert>
    </merge_tree>
</clickhouse>
